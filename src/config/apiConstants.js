/**
 * @file Constants for API configuration.
 * @description Centralizes API-related constants to avoid hardcoded values.
 */

/**
 * HTTP request timeout in milliseconds.
 * @constant {number}
 */
export const API_TIMEOUT = 5000;

/**
 * Default number of random cats to fetch.
 * @constant {number}
 */
export const DEFAULT_CAT_LIMIT = 12;