/**
 * @file LocalStorage keys constants.
 * @description Centralizes localStorage keys to avoid typos and hardcoded strings.
 */

/**
 * Key for storing theme preference.
 * @constant {string}
 */
export const STORAGE_KEY_THEME = "theme";

/**
 * Key for storing font preference.
 * @constant {string}
 */
export const STORAGE_KEY_FONT = "font";